# Render Blueprint - Generated by InoPay NCS 2.0
services:
  - type: web
    name: inopay-frontend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    autoDeploy: false
    scaling:
      minInstances: 1
      maxInstances: 3

  - type: web
    name: inopay-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    autoDeploy: false

databases:
  - name: inopay-db
    plan: starter
    databaseName: inopay_db
